# Server
server.port=8085

# H2 Database
#spring.datasource.url=jdbc:h2:mem:authdb;DB_CLOSE_DELAY=-1
#spring.datasource.driverClassName=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
# MySQL Database (moved to docker-compose)
#spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/authdb?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
#spring.datasource.username=${DB_USERNAME:root}
#spring.datasource.password=${DB_PASSWORD:root}

# JPA (moved to docker-compose)
#spring.jpa.hibernate.ddl-auto=create
#spring.jpa.show-sql=true
#spring.jpa.defer-datasource-initialization=true

# H2 Console
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console

# SQL Initialization (moved to docker-compose)
#spring.sql.init.mode=always
#spring.sql.init.data-locations=classpath:data.sql

# JWT
jwt.secret=your-secret-key-for-jwt-token-signing
jwt.expiration=86400000

# Logging
logging.level.com.example.auth=DEBUG
logging.level.org.springframework.security=DEBUG

# Eureka
eureka.client.service-url.defaultZone=http://discovery-server:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,refresh
management.endpoint.health.show-details=always

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
management.tracing.sampling.probability=1.0
management.zipkin.tracing.endpoint=http://zipkin:9411/api/v2/spans
management.tracing.propagation.type=w3c
